extends layouts/main-layout.pug

block styles
    link(rel="stylesheet", href="/css/product.css")

block content
    main
        if user.length > 0
            .grid
                each person in user
                    article.card.product-item
                        header.card__header
                            h1.product__title #{person.title}
        else
            h1 No Users